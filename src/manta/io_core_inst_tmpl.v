/* MODULE_NAME */ /* INST_NAME */ (
    .clk(clk),

    // ports
    /* INST_PORTS */

    // input port
    .addr_i(),
    .wdata_i(),
    .rdata_i(),
    .rw_i(),
    .valid_i(),

    // output port
    .addr_o(),
    .wdata_o(),
    .rdata_o(),
    .rw_o(),
    .valid_o()
    );