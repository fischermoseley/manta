/* MODULE_NAME */ /* INST_NAME */ (
    .clk(clk),

    // ports
    /* INST_PORTS */

    // input port
    .addr_i(),
    .data_i(),
    .rw_i(),
    .valid_i(),

    // output port
    .addr_o(),
    .data_o(),
    .rw_o(),
    .valid_o());