/* OP_DEC */
/* ARG_DEC */
/* TRIG_DEC */

trigger #(.INPUT_WIDTH(/* INPUT_WIDTH */)) /* NAME */ (
    .clk(clk),

    .probe(/* PROBE */),
    .op(/* OP */),
    .arg(/* ARG */),
    .trig(/* TRIG */));
